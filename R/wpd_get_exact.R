wpd_get_exact <-
  function(page = NULL, lang = NULL){

    # check for languages in set of supported languages
    lang <- lang[lang %in% wpd_languages]

    # loop over languages and get matches
    for ( l in lang ){

    }
  }